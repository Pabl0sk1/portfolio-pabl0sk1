---
const {} = Astro.props;
---

<form
  name="contact"
  method="POST"
  action="/thanks"
  data-netlify="true"
  netlify-honeypot="bot-field"
  data-netlify-recaptcha="true"
  enctype="multipart/form-data"
  class="flex flex-col gap-4 bg-white/5 p-6 rounded-xl shadow-lg"
>
  <input type="hidden" name="form-name" value="contact" />
  <input type="hidden" name="bot-field" />

  <div class="flex flex-col">
    <label class="text-sm font-medium text-gray-300">Nombre</label>
    <input
      type="text"
      name="name"
      class="mt-2 p-2 rounded-lg bg-white/10 text-white"
      required
    />
  </div>

  <div class="flex flex-col">
    <label class="text-sm font-medium text-gray-300">Correo</label>
    <input
      type="email"
      name="email"
      class="mt-2 p-2 rounded-lg bg-white/10 text-white"
      required
    />
  </div>

  <div class="flex flex-col">
    <label class="text-sm font-medium text-gray-300">Asunto</label>
    <input
      type="text"
      name="subject"
      class="mt-2 p-2 rounded-lg bg-white/10 text-white"
      required
    />
  </div>

  <div class="flex flex-col">
    <label class="text-sm font-medium text-gray-300">Mensaje</label>
    <textarea
      name="message"
      class="mt-2 p-2 rounded-lg bg-white/10 text-white"
      rows="5"
      required></textarea>
  </div>

  <div class="flex flex-col">
    <label class="text-sm font-medium text-gray-300 mb-2"
      >Adjuntar archivo</label
    >
    <label
      class="flex justify-between items-center w-full text-sm px-4 py-3 bg-white/10 rounded-lg cursor-pointer border border-dashed border-gray-400 hover:border-blue-400 hover:bg-white/20 transition"
    >
      <span id="file-name" class="truncate">Selecciona un archivo...</span>
      <input
        type="file"
        name="resume"
        class="hidden"
        onchange="document.getElementById('file-name').textContent = this.files[0]?.name || 'Selecciona un archivo...'"
      />
    </label>
  </div>

  <div data-netlify-recaptcha="true" class="mt-4"></div>

  <button
    type="submit"
    class="mt-4 bg-blue-500 text-white font-bold py-2 rounded-lg hover:bg-blue-400 transition-all cursor-pointer"
  >
    Enviar Mensaje
  </button>
</form>

---
import { getLangFromUrl } from "../i18n/utils";
import { aboutMe, aboutMeImageAlt } from "../i18n/content";

const lang = getLangFromUrl(Astro.url);
const parrafos = aboutMe[lang];
---

<article
  class="flex flex-col items-center justify-center gap-8 text-gray-700 dark:text-gray-300 md:flex-row"
>
  <div
    class="[&>p]:mb-4 [&>p>strong]:text-blue-500 dark:[&>p>strong]:text-blue-200 [&>p>strong]:font-normal [&>p>strong]:font-mono text-pretty order-2 md:order-1"
  >
    {parrafos.map((parrafo) => <p set:html={parrafo} />)}
  </div>
  <img
    width="256"
    height="256"
    loading="lazy"
    decoding="async"
    src="/me.webp"
    alt={aboutMeImageAlt[lang]}
    class="order-1 object-cover w-64 h-full p-1 md:order-2 rotate-3 lg:p-2 lg:w-64 aspect-square rounded-2xl bg-black/20 dark:bg-yellow-500/5 ring-1 ring-black/70 dark:ring-white/20"
    style="object-position: 50% 50%"
  />
</article>
